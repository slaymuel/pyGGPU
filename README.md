# (WIP) LLVM-based JIT compiler and runtime for multi-backend (CPU and GPU) python kernels

The goal of pyGGpu is to be a (almost type safe) multi-backend JIT kernel engine that compiles Python functions directly to GPU and CPU (PTX, CPU, SPIR-V etc) targets using LLVM. 
